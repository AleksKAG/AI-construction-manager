services:
  app:
    build:
      context: .
      dockerfile: deployments/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ENV: production
      PORT: 8080
      JWT_SECRET: supersecretkey1234567890changeinproduction2026
    restart: unless-stopped
    volumes:
      - ./data:/app/data
